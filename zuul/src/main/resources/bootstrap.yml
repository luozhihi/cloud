spring:
  application:
    name: zuul
  cloud:
    config:
      label: master
      discovery:
        enabled: true
        service-id: config-server
      profile: test
      name: application
server:
  port: 8083
eureka:
  client:
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:80/eureka/
